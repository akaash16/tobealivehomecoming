import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Hides the floating dev indicator in development (production is unaffected). */
  devIndicators: false,
};

export default nextConfig;
